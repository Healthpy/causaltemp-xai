# Stage 1: [Name]

**Goal**: [What this stage accomplishes -- one sentence]
**Dependencies**: [Stage ids that must be DONE first, or "None"]

---

## Steps

1. [Specific action]
   - Where: `SymbolName` in `path/to/file` -- anchor on a symbol, not a bare line number
   - Details: [What to change]

---

## Verification

Each line is `GATE` (must pass) or `REPORT` (measure and record in `journal.md`).
A measured GATE names the input its value is read from and the defect that turns it red -- a line
that only names a command passes whenever the command exits 0.

- [ ] GATE [test command] -- passes
- [ ] GATE [measured value] -- read from [named input]; [named defect] turns it red
- [ ] REPORT [measurement command] -- record the value in `journal.md`

---

## Commit

`type(scope): description`
